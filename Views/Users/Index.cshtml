@model IEnumerable<MVCApplication.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<h1>
    Welcome @ViewBag.name;
</h1>
<h1>
    You are From @ViewBag.city;
</h1>*@
<div>
    <p>
        @Html.ActionLink("Create New", "AddNewUser")
    </p>
    <p align="right">
        @Html.ActionLink("Careers", "Careers")
    </p>
</div>
<table class="table" style="border: 3px solid red">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.username)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.password)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th>Select Here</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.username)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.password)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>

            <td>
                @Html.ActionLink("Edit", "UpdateUser", new { id = item.userid }) |
                @Html.ActionLink("Details", "UserDetails", new { id = item.userid }) |
                @Html.ActionLink("Delete", "DeleteUser", new { id = item.userid })
            </td>
        </tr>
    }
</table>
<p>
    @Html.ActionLink("Go to Page2", "Print")
</p>

    @*<h2>List Of Countries</h2>
    <ol>
        @foreach (string str in ViewBag.Countries)
        {
            <li> @str</li>

            <br />
        }
        @foreach (string str in (List<string>)ViewData["Countries"])
        {
            <li> @str</li>

            <br />
        }
    </ol>
    <h2>
        welcome @ViewData["name"]
        You are From @ViewData["city"];
    </h2>*@

